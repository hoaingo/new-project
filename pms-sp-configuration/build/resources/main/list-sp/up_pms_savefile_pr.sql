-- CREATE DEFINER=`intern`@`%` PROCEDURE `up_pms_savefile_pr`(pData longblob,  pFile_name varchar(45), pFile_type varchar(100), pCreated_date date, pCreated_by varchar(45))
-- BEGIN
-- 		INSERT INTO pms_document(
-- 								`data`,    
-- 								`file_name`,
--                                 `file_type`,
-- 								`created_date`,
--                                 `created_by`
--                             )
-- 		VALUES
-- 						(  pData, pFile_name, pFile_type, Now(),pCreated_by);
--                         
-- 		SELECT * from `pms_document` ORDER BY document_id DESC LIMIT 1;
-- END